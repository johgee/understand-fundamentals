learn RUST 

fn main() {
  printIn!("hello, world!");
}

copy & paste the link from the RUST website into your terminal to download the program 
cargo new hello-rust = hello-rust name of new project 

*adding dependencies 
packages => crates  = additional source code other people wrote 

user ferris_says::say;
use std::io::{stdout, BufWriter};

fn main() {
  let stdout = stdout();
  let out = b"Hello frmo Ferris!";
  let width = 50;

  let mut writer = BufWriter::new(stdout.lock());
  say(out, width, &mut writer).unwrap();
}